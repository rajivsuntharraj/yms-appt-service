apiVersion: v1
kind: Service
metadata:
  name: {{ include "yms-appt-service.fullname" . }}
  labels:
    {{- include "yms-appt-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "yms-appt-service.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http

